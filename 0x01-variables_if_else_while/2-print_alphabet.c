#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 
 */
int main(void)
{
	char ch = 'a';
	while (ch <= 'z')x;
	{
	putchar(ch);
	chx++;
	putchar("\n");
	}
	return(0);
}

